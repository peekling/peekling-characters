# X3 source artwork

X3 is rendered from deterministic geometric primitives at 1x, 2x, and 4x. The renderer creates the terminal body, screen, keyboard, and two expressive cyan LED eyes directly at each density. The screen does not display letters, numbers, or other identity text.

The packaged atlases contain original generated artwork. They do not contain pixels copied from a reference image.
